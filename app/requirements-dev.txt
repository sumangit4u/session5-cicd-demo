# Test-only dependencies - installed in CI, never baked into the Docker image.
pytest==8.3.4
# google-genai 1.5.0 (in requirements.txt) needs httpx>=0.28.1, so the pin
# here must stay compatible with it or pip's resolver fails in clean CI runs.
httpx==0.28.1
